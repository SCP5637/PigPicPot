<Window x:Class="PigPicPot.Core.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        Width="400" Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Name="PromptText" Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <TextBox Name="InputTextBox" Grid.Row="1" Margin="0,0,0,15" Padding="3"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="确定" Width="75" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="取消" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>