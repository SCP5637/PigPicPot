<Window x:Class="PigPicPot.Core.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" 
        Width="400" Height="130"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Name="MessageText" Grid.Row="0" Margin="0,0,0,20" TextWrapping="Wrap"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="YesButton" Content="是" Width="75" Margin="0,0,10,0" Click="YesButton_Click"/>
            <Button Name="NoButton" Content="否" Width="75" Click="NoButton_Click"/>
        </StackPanel>
    </Grid>
</Window>